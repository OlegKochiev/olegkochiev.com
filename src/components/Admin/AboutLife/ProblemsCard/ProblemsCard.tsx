import React from 'react';
import {Card, CardTitle, CardBody, CardFooter} from '../../../UI/Card/StyledCard';

export default function ProblemsCard() {
  return (
    <Card>
      <CardTitle>Some title</CardTitle>
      <CardBody></CardBody>
      <CardFooter></CardFooter>
    </Card>
  );
}
